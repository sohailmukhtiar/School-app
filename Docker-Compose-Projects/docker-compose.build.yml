version: "3.8"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: sohail185/backend-app:${IMAGE_TAG}

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: sohail185/frontend-app:${IMAGE_TAG}
